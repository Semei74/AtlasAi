import { ReactElement } from 'react';
import { ComingSoon } from '../../../../frontend/src/screens';

export default function SecurityRoute(): ReactElement {
  return <ComingSoon feature="Security Settings" />;
}
